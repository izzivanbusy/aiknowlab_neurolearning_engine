[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m alembic upgrade head && python -m app.modules.german_vocab_a1.seed && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
