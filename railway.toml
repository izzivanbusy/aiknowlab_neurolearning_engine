[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m alembic upgrade head && python -m app.modules.german_job_interview.seed && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
